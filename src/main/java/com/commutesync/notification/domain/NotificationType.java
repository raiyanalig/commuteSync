package com.commutesync.notification.domain;

public enum NotificationType {
    BOOKING_CONFIRMED,
    BOOKING_CANCELLED,
    TRIP_ASSIGNED,
    TRIP_STARTED,
    TRIP_CANCELLED,
    TRIP_COMPLETED
}
