package com.commutesync.audit.domain;

public enum AuditAction {
    LOGIN,
    LOGIN_FAILED,
    VEHICLE_CREATED,
    TRIP_ASSIGNED,
    TRIP_STARTED,
    TRIP_COMPLETED,
    TRIP_CANCELLED,
    BOOKING_CREATED,
    BOOKING_CANCELLED
}
